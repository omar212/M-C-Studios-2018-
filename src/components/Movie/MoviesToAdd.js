import React, { Component } from 'react';

const MoviesToAdd = (props) => {
    return(
        <div>
    
        </div>
      );
}

export default MoviesToAdd;
//
// export default class Relationship {
//
// }
// export const Id = {this.props.id};
// // export const Name = {props.name};
// // export const Name = {this.props.name};
